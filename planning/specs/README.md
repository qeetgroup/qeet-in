# planning/specs/

Page/campaign plans written by the **content-architect** agent (one file per proposal,
`<slug>.md`). Each spec drives the build pipeline — see [`../../.claude/PIPELINE.md`](../../.claude/PIPELINE.md).
These are internal planning docs — **not** published content (that lives in `src/content/`).
